# Video loops

Optional full-screen or panel backgrounds.

| File | Used by |
|------|---------|
| `hero-bg.mp4` | Landing hero (`HeroBackdrop`) |
| `hub-bg.mp4` | PegKeeper orbit panel (`HubBackdrop`) |

Keep files short, muted, and web-optimized (H.264, ≤ ~5 MB).
